#ifndef TYPES_H
#define TYPES_H

#include <vector>

#define SEED 5061983

typedef enum _event_type {
        SPIKE = 0
} EventType;

typedef unsigned int uint;
typedef double real;
typedef std::vector<double> array;

#endif

